SEOUL_API_KEY=

# Phase 2: override the API base URL to point at apps/api or another backend.
# Leave unset in Phase 1 so client calls hit the Vite dev proxy at /api.
# VITE_API_BASE=https://api.example.com
